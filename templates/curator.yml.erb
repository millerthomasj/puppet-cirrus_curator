### MANAGED BY PUPPET ###
client:
  <% scope['cirrus_curator::config::_client_config'].keys.sort.each do |key| -%>
  <%= key %>: <%= scope['cirrus_curator::config::_client_config'][key] %>
  <% end -%>

logging:
  <% scope['cirrus_curator::config::_logging_config'].keys.sort.each do |key| -%>
  <%= key %>: <%= scope['cirrus_curator::config::_logging_config'][key] %>
  <% end -%>
